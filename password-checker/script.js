let user = prompt("Who is there?");

if(user === "Admin") {
    let password = prompt("Please enter password");
    if(password === "TheMaster") {
        alert("Welcome!!");
    } else if(password === "" || function keyPress (e) {
        if(e.key === "Escape") {
            alert("Canceled");
        }
    }) {
       //alert("Canceled");
    } else {
        alert("Wrong Password!")
    }
} else if(user === "" || null || function keyPress (e) {
    if(e.key === "Escape"){
        
    }}) {
   //alert("Canceled");
} else if(user !== "Admin") {
    alert("I don't know you");
}


